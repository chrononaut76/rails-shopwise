<%= form_with model: @item do |form| %>
  <%= form.text_field :name, data: { search_items_target: 'input', action: 'keyup->search-items#returnResults' } %>
  <%= form.submit 'Add to my items' %>
<% end %>
