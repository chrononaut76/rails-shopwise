<h1>Nearby Stores</h1>

<div>
  <% @total_by_store.each do |h| %>
    <div>
      <h2><%= h[:store] %></h2>
      <p>Total List Price of Items: $<%= h[:total_price] %></p>
    </div>
  <% end %>
</div>


<div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
